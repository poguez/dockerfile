# FUNCTIONS
function os-update() {
  apt-get update
  apt-get upgrade
}

function os-clean() {
  apt-get clean
  apt-get autoclean
  apt-get autoremove
}

function col {
  awk -v col=$1 '{print $col}'
}
